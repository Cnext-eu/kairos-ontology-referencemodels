# Published contract surface — the machine-readable half of CONTRACT.md.
#
# Every file this repository publishes that a consumer parses is listed here, with the loader
# that reads it and the check that keeps it honest. Enforced by tests/test_contract_manifest.py:
# a schema listed here must validate every matching file, and an enforced_by test must exist.
#
# Why the file exists: `data-domains.yaml` became load-bearing without ceremony — it acquired a
# second consumer (reference_modules) and a whole `module_profiles` block in the toolkit while
# nothing here recorded that it was contract at all. Meanwhile `BLUEPRINT.md` sat beside it
# looking equally official and was read by nobody. This file draws that line explicitly.
#
# `schema: null` is honest, not a gap: an XML catalog is not JSON-Schema shaped, and ontology
# modules are validated by execution (the real loader, or resolution) rather than by schema.
# The pattern library gained its owed schema in patterns v0.3.0 — open top level (the toolkit's
# loader is deliberately lenient about custom blocks), strict entry shapes.

contract_version_policy: >
  Versioned with the repository VERSION. A change to anything listed here is a minor bump at
  least and is called out in CHANGELOG.md. Advisory surfaces — warnings, discovery prose,
  model sheets — are not contract and may change freely.

surfaces:
  - id: catalog
    path: catalog-v001.xml
    schema: null
    consumers:
      - kairos_ontology.core.archetype_loader (root marker via _looks_like_refmodels_root)
      - kairos_ontology.core.reference_modules (module document resolution)
    enforced_by:
      - tests/test_model_registration.py::test_every_accelerator_import_is_catalogued
      - tests/test_bundle_conformance.py::test_every_source_resolves_its_import_closure
      - scripts/validate_archetypes.py (check 2, ref_model_modules IRI resolution)
    notes: >
      Both explicit <uri> entries and <rewriteURI> prefix rules are contract. The rewrite rule
      alone covers 300+ FIBO documents; scripts/catalog_utils.py ignored rewrites entirely
      until v1.15.0+, so every FIBO import silently resolved to None.
      tests/test_model_registration.py::test_every_rewrite_prefix_is_a_real_directory
      now statically covers rewritePrefix targets. Resolution is proven by execution too:
      test_bundle_conformance.py loads every bundled TTL through the consumer's own loader,
      which is what caught the missing OMG Commons / LCC / SKOS mirrors in gh#57.

  - id: archetypes
    path: blueprints/archetypes/*.yaml
    schema: blueprints/archetypes/_schema/archetype.schema.json
    consumers:
      - kairos_ontology.core.archetype_loader.load_archetype
    reads:
      - ref_model_modules[].{iri,tier}
      - core_concepts[].{uri,tier,label}
    enforced_by:
      - scripts/validate_archetypes.py
      - tests/test_toolkit_contract.py::test_every_archetype_loads_through_the_real_loader
    notes: >
      $defs/tier is duplicated in the toolkit as archetype_loader.VALID_TIERS. Changing it
      needs a coordinated PR in both repositories;
      test_toolkit_contract.py::test_tier_enum_matches_the_consumer_copy fails when they drift.

  - id: patterns
    path: blueprints/patterns/*/pattern.yaml
    schema: blueprints/patterns/_schema/pattern.schema.json
    consumers:
      - kairos_ontology.core.pattern_loader.load_pattern (fail-fast)
      - kairos_ontology.core.pattern_loader.load_patterns (warn-and-skip)
    enforced_by:
      - tests/test_toolkit_contract.py::test_every_pattern_loads_through_the_real_loader
      - tests/test_toolkit_contract.py::test_bulk_pattern_load_emits_no_warnings
      - scripts/validate_archetypes.py (check 7, multimodal-order-leg mode_bindings)
      - scripts/validate_structure.py (pattern.yaml schema + <pattern-id>.ttl fragment guard)
      - scripts/validate_pattern_conformance.py (derived ontologies vs normative patterns)
    notes: >
      The schema is open at the top level (the toolkit's loader promotes seven known keys and
      preserves the rest in `extra`; custom top-level blocks differ per pattern by design) and
      strict inside list-entry shapes — the v1.13.0 defect was a stray key inside a block
      sequence, invisible to a human and silently skipped by bulk listing, which is why bulk
      load emitting any warning is treated as a failure here. Structured anti-pattern fields
      (banned_name_tokens / applies_to_ranges / exemptions) are contract for the pattern
      conformance check; every exemption entry carries a cited reason. grain_collisions has
      two entry shapes by design: class-anchored collisions use {against, reason} with a
      scalar against IRI (the toolkit coverage ledger keys units on it); the bare-string
      form is reserved for grain warnings that name no class.

  - id: data-domains
    path: accelerator-packs/*/client-hub-blueprint/data-domains.yaml
    schema: accelerator-packs/_schema/data-domains.schema.json
    consumers:
      - kairos_ontology.core.analyse_sources.load_data_domains
      - kairos_ontology.core.analyse_sources.build_data_domain_targets
      - kairos_ontology.core.reference_modules.load_accelerator_module_config
      - kairos_ontology.core.scaffold_binding (owl:imports resolution for a target class)
    reads:
      - groups[].domains[].{id,name,owns,does_not_own}
      - groups[].domains[].imports[].{uri,module,profile}
      - module_profiles[] (optional; absent entries are synthesised as legacy profiles)
    enforced_by:
      - tests/test_model_registration.py::test_every_data_domain_import_is_catalogued
      - tests/test_model_registration.py::test_every_include_reaches_data_domains
    notes: >
      The path is hardcoded in the toolkit (analyse_sources and reference_modules), so moving
      this file is a coordinated two-repository change and must use expand/contract — see
      CONTRACT.md, Deprecation. `owns`/`does_not_own` are classifier input, not decoration.

  - id: entity-projections
    path: accelerator-packs/*/client-hub-blueprint/entity-projections.yaml
    schema: accelerator-packs/_schema/entity-projections.schema.json
    consumers:
      - kairos_ontology.core.propose_alignment (column-cluster recognition, address projection)
    reads:
      - projections[].{id,target_concept,target_candidates,min_complementary_parts}
      - projections[].{relationship_naming,default_relationship,cardinality}
      - projections[].part_kinds[].{kind,tokens,compact,weak,requires}
      - projections[].{role_qualifiers,context_tokens}
    enforced_by:
      - scripts/validate_structure.py (schema + unsatisfiable-configuration checks)
      - tests/test_validate_structure.py::test_every_shipped_entity_projection_validates
      - tests/test_contract_manifest.py::test_declared_schema_validates_every_matching_file
    notes: >
      Optional per pack, and absence is contract too: a pack that ships no file yields NO
      candidates, because the toolkit deliberately has no vocabulary of its own to fall back on
      (DD-188 — ontology semantics live here, never compiled into the engine). Only logistics
      ships one today; financial-services is absent on purpose, its role vocabulary being
      unevidenced rather than empty. The tokens, role_qualifiers and context_tokens ARE the
      detector, so an edit here changes what a hub detects — same class of behavioural file as
      data-domains.yaml's owns/does_not_own prose. It is deliberately paired with that file: same
      client-hub-blueprint/ directory, same shared accelerator-packs/_schema/ for the schema. An
      earlier draft put it under current/blueprint/, which is the logistics blueprint dossier and
      does not exist in financial-services at all — a path the second pack could not use, in a
      change whose whole point is two packs shipping different vocabularies. Pack-scoped document,
      pack-agnostic shape: the schema is authored once. Candidates derived from it are advisory
      (requires_human_confirmation: true) and never change a disposition. Not a
      blueprints/patterns/ entry: a recognition rule has nothing to conform to in a TTL, so
      validate_pattern_conformance.py does not apply.

  - id: discovery-docs
    path: accelerator-packs/*/discovery/*.md
    schema: null
    consumers:
      - kairos_ontology.core.archetype_loader.locate_discovery_doc
    enforced_by:
      - scripts/validate_archetypes.py (checks 3, 5, 6)
    notes: >
      Only the *path* is contract. The toolkit hands it to a design skill and never parses the
      markdown, so the file name must match the archetype id. Content is advisory.

  - id: ontology-modules
    path: [derived-ontologies/, authoritative-ontologies/, blueprints/ontology/]
    schema: null
    consumers:
      - kairos_ontology.core.reference_modules.resolve_reference_modules
    enforced_by:
      - scripts/version_manager.py check (VERSION matches owl:versionInfo)
      - scripts/validate_structure.py
      - tests/test_model_registration.py
      - tests/test_bundle_conformance.py::test_every_source_resolves_its_import_closure
      - tests/test_bundle_conformance.py::test_every_bundled_ttl_is_classified
    notes: >
      Module document IRIs and their owl:versionInfo are contract; a hub resolves modules by
      IRI through the catalog. Authoritative mirrors are vendored verbatim and never edited.
      The *set* of modules is contract too: every non-archived .ttl must be inventoriable by
      the consumer and must map to a unique inventory filename. v1.16.0 shipped two files both
      named template.ttl that collapsed onto one inventory and clobbered each other (gh#57).
